# 生產環境配置示例
# 複製此文件為 .env.production 並填入實際值

# 環境
NODE_ENV=production
PORT=5000

# 數據庫 (Docker PostgreSQL)
DB_PASSWORD=your-very-secure-database-password-at-least-32-characters
DATABASE_URL="postgresql://postgres:${DB_PASSWORD}@postgres:5432/social_media?schema=public&connection_limit=20"

# JWT 配置
JWT_SECRET=your-very-secure-jwt-secret-key-at-least-64-characters-random-string
JWT_EXPIRES_IN=7d

# Supabase (可選 - 用於文件存儲)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# CORS 配置
CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# 日誌級別
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
