# 環境變數配置模板
# 複製此文件為 .env 並填入實際值
# ⚠️ 警告：.env 文件包含敏感信息，絕不要提交到 Git！

NODE_ENV=development
PORT=5000

# JWT 配置
# 生成安全的隨機字符串：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=請替換為至少64字符的隨機字符串
JWT_EXPIRES_IN=7d

# PostgreSQL 數據庫配置
# 
# 開發環境: 使用 Supabase
# 生產環境: 使用 Docker PostgreSQL
#
# Supabase 連接 (開發環境 - Direct Connection)
# 格式: postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD_HERE@db.YOUR_PROJECT_REF.supabase.co:5432/postgres"

# Supabase API Key (用於存儲等功能)
# 從 Supabase Dashboard → Settings → API 獲取
SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY_HERE"

# 生產環境配置 (未來使用 Docker PostgreSQL)
# DATABASE_URL="postgresql://postgres:YOUR_STRONG_PASSWORD@postgres:5432/social_media?schema=public"
